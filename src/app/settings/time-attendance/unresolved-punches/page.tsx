'use client';

import SettingsUnresolvedPunches from '@/components/pages/settings-unresolved-punches';

export default function UnresolvedPunchesPage() {
  return (
    <div className="font-sans min-h-screen">
      <SettingsUnresolvedPunches />
    </div>
  );
}
